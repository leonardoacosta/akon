import Layout from 'components/Layout';
import AvailabilityInfo from 'components/screens/Availability/Info';

export const Availability = () => {
	return (
		<Layout>
			<AvailabilityInfo />
		</Layout>
	);
};

export default Availability;
